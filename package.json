{
  "name": "chessington",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "clean": "del-cli dist/**/* logs/*",
    "bundle": "webpack",
    "build": "npm run clean && babel ./src --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log,./src/engine --copy-files && tsc && npm run bundle",
    "start": "npm run build && node dist/index.js",
    "test": "mocha --require ts-node/register \"./tests/**/*.spec.ts\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.3.1",
    "@types/express": "^4.17.13",
    "@types/mocha": "^9.1.1",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "chai": "^4.1.2",
    "del-cli": "^1.1.0",
    "mocha": "^4.0.1",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3",
    "webpack": "^3.6.0"
  },
  "dependencies": {
    "express": "^4.16.1"
  }
}
